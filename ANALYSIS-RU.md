# Анализ результатов: SHA-256 PoW benchmark

Документ разбирает референсные прогоны из `results/bench-oniguruma-*.json`
и интерпретирует их через минимальный комплект формул из теории
вероятностей. Аудитория — инженеры, которым из прикладной математики
нужны только те утверждения, которые используются дальше по тексту.

## 1. Постановка измерения

Задача solver'а — найти такой целочисленный nonce, чтобы первые `N`
hex-разрядов `sha256(token || ascii_decimal(nonce))` были нулями.
Каждый перебранный nonce — независимое испытание Бернулли. Параметр
сложности управляет одной величиной — вероятностью успеха:

$$
p_N = 16^{-N}
$$

Всё, что мы измеряем, — следствие из этого факта плюс скорости
хэширования $r$ (hashes/sec).

Headline-метрика бенчмарка: максимальный `N`, при котором медианное
время решения укладывается в заданный бюджет (60 секунд в reference-прогонах).
Эквивалентная формулировка — наибольший $N$, при котором

$$
\Pr(T \le t_{\mathrm{target}}) \ge \frac{1}{2}
$$

Ноутбук рядом печатает эту вероятность по каждой ячейке с биномиальным
интервалом Уилсона, чтобы у близких к границе прогонов не возникало
иллюзии точности.

## 2. Математические вводные

### 2.1 Распределение числа попыток

Число попыток до первого успеха — геометрическая величина:

$$
A \sim \mathrm{Geom}(p_N)
$$

Для уровня сложности $N$:

$$
\begin{aligned}
\Pr(A = k) &= (1 - p_N)^{k - 1} p_N \\
\mathbb{E}[A] &= \frac{1}{p_N} = 16^N \\
\mathrm{Var}(A) &= \frac{1 - p_N}{p_N^2} \approx 16^{2N} \\
\frac{\sigma(A)}{\mathbb{E}[A]} &= \sqrt{1 - p_N} \approx 1
\end{aligned}
$$

Отсюда первый практический факт: разброс $A$ имеет тот же порядок,
что и его среднее. Удвоение или утроение времени решения относительно
ожидаемого — норма, а не аномалия.

### 2.2 Переход к wall-clock

Время решения:

$$
T = \frac{A}{r}
$$

При фиксированной скорости $r$ распределение $T$ дискретное, но при
$p_N \ll 1$ оно близко к непрерывному экспоненциальному:

$$
\begin{aligned}
T &\sim \mathrm{Exp}(\lambda_N) \\
\lambda_N &= \frac{r}{16^N} \\
F(t) &= 1 - e^{-\lambda_N t}
\end{aligned}
$$

Относительная ошибка приближения:

$$
O(p_N) = O(16^{-N})
$$

Поэтому при $N \ge 4$ разница между геометрическим и экспоненциальным распределением
исчезает на фоне выборочного шума.

### 2.3 Почему экспонента, а не нормальное

Интуиция «много независимых попыток → колокол» опирается на ЦПТ:
сумма большого числа независимых вкладов асимптотически нормальна.
В нашей задаче усреднения нет. Мы наблюдаем момент *первого* успеха
в потоке независимых бернуллиевых испытаний — это first-passage,
не сумма.

Эквивалентно: успехи приходят как пуассоновский процесс с
интенсивностью $\lambda$, и время до первого события пуассоновского
процесса — это по определению:

$$
\mathrm{Exp}(\lambda)
$$

Никакая колоколообразная форма из этой постановки получиться не может.

Memorylessness объясняет, почему дисперсия не «сглаживается с
опытом»: после $s$ секунд бесплодного поиска остаточное время остаётся
распределённым как

$$
\mathrm{Exp}(\lambda)
$$

Каждая попытка независима, накопленной информации нет. $16^N$ попыток
в среднем — это не $16^N$ слагаемых в дисперсии, а $16^N$ способов
*не* успеть до сих пор; вся
стохастика задачи приходится на единственный успешный исход.

Норма всё-таки появляется в наших же данных, но в задачах с
усреднением:

- Калибровочный hashrate распределён примерно нормально вокруг
  ~1.2 GH/s — за две секунды складываются миллиарды мелких
  независимых интервалов между хэшами, и ЦПТ работает буквально.
- Время найти $K$ решений подряд при больших $K$ есть сумма
  экспоненциальных величин:

  $$
  \sum_{i=1}^{K} \mathrm{Exp}(\lambda) = \Gamma(K, \lambda),
  \qquad
  \mathrm{CV} \approx \frac{1}{\sqrt{K}}
  $$

  Если бы headline требовал «найти 100 решений
  с четырьмя нулями» вместо «одно решение с восемью»,
  эмпирические времена образовали бы колокол с $\mathrm{CV} \approx 0.1$,
  а не экспоненту с $\mathrm{CV} = 1$. Та же физика, другой момент
  остановки.

Короткий критерий, какое распределение ожидать:

- сумма/среднее $n$ независимых компонентов → нормальное (ЦПТ);
- время до первого редкого события → экспоненциальное (Пуассон);
- время до $K$-го события при больших $K$ → опять нормальное,
  через гамму.

PoW попадает в средний случай: ровно одно редкое событие, и в нём
вся стохастика задачи. Увеличение `runs` не меняет формы
распределения — оно сужает доверительный интервал на оценку $\lambda$,
но не превращает экспоненту в колокол.

### 2.4 Характеристики $\mathrm{Exp}(\lambda)$, которые нужны для интерпретации

$$
\begin{aligned}
\mathbb{E}[T] &= \frac{1}{\lambda} = \frac{16^N}{r} \\
\mathrm{median}(T) &= \frac{\ln 2}{\lambda} \approx 0.693 \cdot \mathbb{E}[T] \\
p_{95}(T) &= \frac{-\ln 0.05}{\lambda} \approx 2.996 \cdot \mathbb{E}[T] \\
p_{99}(T) &= \frac{-\ln 0.01}{\lambda} \approx 4.605 \cdot \mathbb{E}[T] \\
\sigma(T) &= \frac{1}{\lambda} = \mathbb{E}[T] \\
\mathrm{CV} &= \frac{\sigma(T)}{\mathbb{E}[T]} = 1 \\
\frac{p_{95}(T)}{\mathrm{median}(T)} &= \frac{\ln 20}{\ln 2} \approx 4.32 \\
\frac{p_{99}(T)}{\mathrm{median}(T)} &= \frac{\ln 100}{\ln 2} \approx 6.64
\end{aligned}
$$

Из этих соотношений следуют две проверки качества измерения:
коэффициент вариации эмпирических $T$ должен быть около 1, отношение
$p_{95}$ к медиане — около 4.32. Их выполнение — индикатор того, что
solver не вносит ничего, кроме линейного масштабирования по $r$.

### 2.5 Почему headline-метрика на медиане

Среднее $\mathrm{Exp}(\lambda)$ имеет дисперсию того же порядка, что само среднее.
Оценка среднего по `n` сэмплам сходится медленно: стандартная ошибка
падает как

$$
\frac{1}{\sqrt{n}}
$$

а в reference-прогонах `n` доходит до 1 на
большом `N` из-за per-N бюджета. Медиана же — порядковая статистика;
её оценка устойчива к одиночным выбросам и не требует допущения о
конечных моментах. Headline:

$$
\max N \quad \text{such that} \quad \mathrm{median}(T_N) \le 60\,\mathrm{s}
$$

Интерпретация: «успеть с вероятностью не меньше $1/2$».

## 3. Источники данных

[1] `results/bench-oniguruma-20260525T120719Z-shani.json` — Rust+SHA-NI,
32 потока rayon, AMD Ryzen 9 5950X (Zen 3). Калибровка: single-thread
56,3 MH/s, parallel 1 207 MH/s.

[2] `results/bench-oniguruma-20260525T132118Z-python.json` — Python
`hashlib`, 1 поток, тот же хост. Калибровка: 1,73 MH/s.

Оба файла соответствуют схеме v2 (`single_hps`, `parallel_hps` в
`calibration`). Прогоны независимы по token (random 16 байт) и
start_nonce (random `u64` для Rust, random 32-bit для Python).

## 4. Сводная таблица

Для каждой пары (impl, N) — число прогонов, nearest-rank медиана и среднее $T$,
$p_{95}$, выборочное $\sigma$, коэффициент вариации, отношение $p_{95}$ к
медиане и эффективная hashrate:

$$
r_{\mathrm{eff}} =
\frac{\sum \mathrm{attempts}}{\sum \mathrm{elapsed}}
$$

В самом ноутбуке
p95 и σ скрываются при `runs < 20`: на такой выборке эти статистики
определяются единичными выбросами, а не распределением. Медиана и
перцентили считаются по тому же правилу nearest-rank, что и в Rust/Python
bench; это устраняет расхождение между notebook-таблицами и записанным
`stats.median_elapsed`/`stats.p95_elapsed`.

| impl   | N | runs | median, s | mean, s | p95, s | σ, s   | CV   | p95/med | eff_hps  |
| ------ | -:| ----:| ---------:| -------:| ------:| ------:| ----:| -------:| --------:|
| shani  | 6 |  30  |   0.032   |  0.046  | 0.119  | 0.041  | 0.90 |  3.71   | 397 MH/s |
| shani  | 7 |  30  |   0.502   |  0.717  | 2.544  | 0.784  | 1.09 |  5.07   | 399 MH/s |
| shani  | 8 |  20  |   6.023   | 10.031  | 34.735 | 11.19  | 1.12 |  5.77   | 403 MH/s |
| shani  | 9 |   1  | 255.184   | 255.184 |   —    |   —    |  —   |    —    | 406 MH/s |
| python | 4 |  30  |   0.035   |  0.057  | 0.168  | 0.049  | 0.87 |  4.76   | 1.33 MH/s|
| python | 5 |  30  |   0.553   |  0.813  | 2.466  | 0.666  | 0.82 |  4.46   | 1.33 MH/s|
| python | 6 |  13  |   8.220   | 14.954  |   —    |   —    |  —   |    —    | 1.34 MH/s|
| python | 7 |   5  |  71.889   | 69.629  |   —    |   —    |  —   |    —    | 1.33 MH/s|

Эффективная hashrate стабильна по `N` для обеих реализаций (±2%).
Это первое подтверждение модели: workload равномерный, и `N` влияет
только на число попыток, а не на стоимость одной попытки.

## 5. Соответствие модели $\mathrm{Exp}(\lambda)$

CV для всех ячеек с $n \ge 20$ лежит в диапазоне 0,82–1,12 при
теоретическом значении 1. Отношение $p_{95}$ к медиане для тех же ячеек —
3,71–5,77 при теоретическом 4,32. Систематических отклонений нет:
крайние значения объясняются конечным размером выборки (для
$n = 20$ стандартная ошибка $p_{95}$ уже составляет десятки процентов от
самой оценки).

Случаи $n < 20$ (shani N=9 имеет 1, python N=6 имеет 13, python N=7
имеет 5) лежат в зоне шумных оценок. В графиках они показаны полыми
маркерами без whiskers, bootstrap-CI и соединения в тренд. По ним
осмысленно опираться только на медиану; все остальные статистики там
диагностические, а не определяющие.

Нормированные ECDF/PDF в ноутбуке сравнивают реализации на оси:

$$
x = \lambda_{\mathrm{eff}} t
$$

При корректной модели формы должны ложиться на

$$
\mathrm{Exp}(1)
$$

независимо от абсолютной скорости реализации. shani N=6
выглядит близко к этой форме; python N=6 помечен как low-n (`13/30`)
и показывает более шумный правый хвост. Источник джиттера локализуется
отдельным профилированием — в текущем прогоне такие измерения не сняты.

## 6. Headline-метрика и связь с hashrate

shani достигает `max N = 8`, python — `max N = 6`. Разница в 2 hex-
разряда соответствует множителю:

$$
16^2 = 256
$$

Соотношение эффективных hashrate:

$$
\frac{397}{1.33} \approx 298
$$

Эти две цифры согласованы:

$$
\log_{16}(298) \approx 2.04
$$

При равных бюджетах прирост hashrate в $K$ раз ровно конвертируется
в прирост `max N` на

$$
\log_{16}(K)
$$

Округление вниз до целого — единственный источник
небольшой нелинейности.

На фиксированном хосте `max N` — логарифмическая по hashrate
величина. Удвоить hashrate — это +0,25 hex-разряда; чтобы добавить
один целый разряд, требуется ускорение в 16 раз. Этим объясняется,
почему на одной машине SHA-NI с 32 потоками даёт всего +2 разряда
против однопоточного Python, несмотря на 700× разницу в калибровке.

## 7. Калибровка против эффективной hashrate

shani в калибровке выдаёт 1 207 MH/s parallel, в solve — 397–406
MH/s. Соотношение 0,33, то есть **67% времени параллельный solver
тратит не на хэширование**. python в калибровке выдаёт 1 730 MH/s,
в solve — 1 330 MH/s. Соотношение 0,77, налог однопоточного solver'а
на координацию — 23%.

В ноутбуке эти два числа теперь явно разнесены. Основной график рисует
две теоретические линии на каждую реализацию: пунктиром — нижнюю границу
чистой `sha256` петли,

$$
\frac{\ln 2 \cdot 16^N}{r_{\mathrm{calib}}}
$$

и штриховой — оценку по pooled effective rate,

$$
\frac{\ln 2 \cdot 16^N}{r_{\mathrm{eff}}},
\qquad
r_{\mathrm{eff}} =
\frac{\sum \mathrm{attempts}}{\sum \mathrm{elapsed}}
$$

по всем прогонам данной реализации. Эмпирическая медиана с bootstrap-CI
ложится близко к штриховой линии и далеко выше пунктирной; вертикальный
зазор между ними — тот самый налог на координацию. Диагностический блок
в отдельной ячейке нормирует три величины:

$$
\frac{\mathrm{mean}(A)}{16^N},
\qquad
\frac{\mathrm{median}(T)}{\ln 2 \cdot 16^N / r_{\mathrm{calib}}},
\qquad
\frac{r_{\mathrm{eff}}}{r_{\mathrm{calib}}}
$$

Они разделяют «модель неправильная» и «solver тяжелее модели».

python однопоточный, и overhead над голым `hashlib.sha256` сводится
к проверке байтового префикса digest и инкременту nonce — обе операции
линейны по числу попыток, без скрытых синхронизаций. shani параллельный,
и его 67% потерь распадаются на несколько составляющих, которые в
текущем прогоне не разделены:

- запуск `rayon::scope` на каждом solve (миллисекундный масштаб);
- atomic-poll shared флага `found` каждые 4096 хэшей в 32 потоках;
- `Arc<Mutex<Option<...>>>` для записи решения и пробуждение чтения
  завершающих потоков;
- незавершённый последний батч у 31 «лишних» потоков после того,
  как один поток нашёл решение;
- аллокация `token.to_vec()` для каждого потока при spawn.

Декомпозиция этого 67% — задача профилирования; в данных её нет.
До профайлера цифра 67% — единственная агрегированная оценка налога
на параллелизм для текущего solver'а.

## 8. Per-N бюджет и его искажающий эффект

Бенчмарк ограничивает суммарное wall-clock на один $N$ величиной:

$$
\max(3 \cdot t_{\mathrm{target}}, 30\,\mathrm{s}) = 180\,\mathrm{s}
$$

Это объясняет, почему
число прогонов падает к границе допустимого `N`: shani N=8 успевает
20 из 30 прогонов, shani N=9 — один. Аналогично python обрывается на
N=6 (13 прогонов) и N=7 (5 прогонов).

Ограничение делает медиану устойчивой, но статистики хвоста — нет.
$p_{95}$ при $n = 5$ оценивается на максимальном значении выборки, что
эквивалентно «p80 ± шум». Headline остаётся честным, диагностические
колонки $p_{95}$/$p_{99}$ рядом с границей применимы только как «есть/нет
сигнала о тяжёлом хвосте».

## 9. Сравнение реализаций на пересечении

На `N = 6` доступны обе реализации одновременно. Медианы: shani
0,032 с, python 8,22 с. Прямое ускорение shani относительно python
на одном `N` — 242×. На том же `N` ECDF в ноутбуке (cell 11) для
обеих реализаций строится на нормированной оси:

$$
\lambda_{\mathrm{eff}} t
$$

shani почти ложится на $\mathrm{Exp}(1)$; python заметно шумнее, и его выборка на
этом `N` неполная (`13/30`). Дистанция до калибровочной пунктирной
кривой согласуется с тем же overhead, который мы видели в разделе 7.

## 10. Что измерение не покрывает

Один хост — нет сравнения между машинами и оценки разброса между
машинами одного класса. Reference-прогон не фиксирует CPU governor,
turbo-state и микрокод программно; снимок `env-*.txt` для этих
файлов в репозитории отсутствует, что мешает воспроизводимости на
другой стороне. Калибровка измеряет «голый» цикл sha256 — она не
содержит координации, поэтому переоценивает rate для параллельного
solve на коэффициент 3. Корректно интерпретировать `calibration.parallel_hps`
как нижнюю границу теоретического минимума:

$$
\mathrm{median}(T_N) =
\frac{\ln 2 \cdot 16^N}{r}
$$

эмпирические медианы лежат на 3 раза выше этой границы для shani и
на 30% — для python.

Список открытых вопросов короткий: декомпозиция 67% overhead'а
параллельного solver'а и природа тяжёлого правого хвоста у python.
Оба решаются стандартным профилированием — `perf` для Rust,
`py-spy` или `cProfile` для Python — но в текущих данных их нет.

## 11. Что делать дальше, если хочется точнее

Для разделения координационных затрат: добавить в solver счётчики
`hashes_per_batch`, `flag_polls`, `rayon_scope_setup_ns` и записывать
их в JSON. Это даст ту же декомпозицию, что обещают пять пунктов в
разделе 7, без отдельного профайлера.

Для устойчивых хвостовых оценок: увеличить `runs` по N, где сейчас
$n < 20$. Точечные $p_{95}$/$\sigma$ в этих ячейках ноутбук уже маскирует
(`NaN`), на основной картинке такие buckets показаны полыми
median-only маркерами; для достаточно больших выборок усы
интерпретируются как сырой spread наблюдений p2.5–p97.5, а не как
доверительный интервал. Для медианы показан bootstrap-CI только там,
где размер выборки позволяет не выдавать шум за устойчивый тренд.
Дополнительный шаг — bootstrap-CI на $p_{95}$ с порогом по относительной
ширине — остаётся открытым, но не блокирует headline.

Для воспроизводимости между машинами: коммитить пару
`bench-*.json` + `env-*.txt` атомарно; добавить в env-снимок
`/sys/devices/system/cpu/intel_pstate/no_turbo` (Linux) или
эквивалент на macOS. Тогда числа с двух машин сравниваются прямо.
